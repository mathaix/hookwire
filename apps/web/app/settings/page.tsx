import { SectionPage } from "../section-page";

export default function SettingsPage() {
  return <SectionPage active="settings" description="Prepare organization, project, and identity controls for onboarding." title="Settings" />;
}
